<div class="container">
  <h3><small>Username:</small> {{username}}</h3>
  <h3><small>Email:</small> {{email}}</h3>
</div>
<div class="container">
	<h4>Stories submited:</h4>
	<ul>
	{{#each story in stories}}
		<li>{{story.title}}</li>
	{{/each}}
	</ul>
</div>